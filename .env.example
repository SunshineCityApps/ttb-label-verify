# Copy to .env.local and fill in with your Anthropic API key.
ANTHROPIC_API_KEY=your-anthropic-api-key

# Optional: override the extraction model (default: claude-haiku-4-5, chosen for the <5s target).
# CLAUDE_MODEL=claude-sonnet-5
